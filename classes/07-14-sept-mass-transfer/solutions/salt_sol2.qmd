# Salt solution - implementation

```{.python filename="salt_mod.py"}
{{< include salt_mod.py >}}
```

Here are some predictions.

```{python}
import numpy as np
import salt_mod as sm

# If needed (common!) use reload function to reload module after editing it
from importlib import reload
reload(sm)

sm.salty(
    conc_init=35, 
    conc_river=0.01, 
    to_rate=3.6e-5, 
    times = np.linspace(0, 1000)
)
```

